general:
	- input: destination
	- settings file
	- commando line options
	- add follow links
	- improve config
	- improve remove_path and set_destination in order to prevent temp. copies
	- add options "(don't) create index file" and "read-able timestamp"
	- set temp. file name of config/log file as constant
	- add "--config-path" option to set path of config files
	- add support for * in paths
	- remove behavior settings
	- change program design (no exceptions)
